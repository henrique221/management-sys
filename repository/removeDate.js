const {selectProgresso ,removeProgresso} = require("./src/models/model")
class removeProgress{
    constructor(date, idProgress){
        this.date = date
        this.idProgress = idProgress
    }

    static remove (date, idProgress) {
        
    }
}
